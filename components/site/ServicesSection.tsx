import Link from "next/link";
import { Plane, Briefcase, GraduationCap, Building2, Heart, Navigation, ArrowRight, type LucideIcon } from "lucide-react";
import { prisma } from "@/lib/prisma";
import { Button } from "@/components/ui/Button";
const iconMap: Record<string, LucideIcon> = { Plane, Briefcase, GraduationCap, Building2, Heart, Navigation };
export async function ServicesSection() { let services: Awaited<ReturnType<typeof prisma.service.findMany>> = []; try { services = await prisma.service.findMany({ where: { published: true }, orderBy: { order: "asc" }, take: 6 }); } catch { services = []; } return <section className="cinematic-section border-t border-brand-line py-24 sm:py-32"><div className="container-page"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Where will you go?</p><h2 className="mt-4 text-5xl font-medium text-brand-secondary sm:text-7xl">Our <em className="text-brand-primary">expertise.</em></h2></div><p className="max-w-sm text-sm leading-relaxed text-brand-muted">A tailored route for every kind of arrival.</p></div><div className="mt-14 grid gap-px bg-brand-line sm:grid-cols-2 lg:grid-cols-3">{services.map((service, index) => { const Icon = iconMap[service.icon ?? ""] ?? Plane; return <article key={service.id} className="group flex min-h-64 flex-col bg-brand-dark p-7 transition-colors hover:bg-brand-surface"><div className="flex items-start justify-between"><Icon className="size-6 text-brand-primary" /><span className="font-mono text-xs text-brand-muted">0{index + 1}</span></div><h3 className="mt-auto pt-10 text-2xl font-medium text-brand-secondary">{service.title}</h3><p className="mt-3 text-sm leading-relaxed text-brand-muted">{service.description}</p><Link href="/contact" className="mt-5 inline-flex items-center gap-2 text-xs uppercase tracking-[0.16em] text-brand-primary">Learn more <ArrowRight className="size-4 transition-transform group-hover:translate-x-1" /></Link></article>; })}</div><div className="mt-12"><Link href="/services"><Button variant="outline" size="lg" className="rounded-none border-brand-primary/50 text-brand-secondary hover:bg-brand-primary hover:text-brand-dark">View all services</Button></Link></div></div></section>; }
